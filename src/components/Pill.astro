---
// Your component script here!
const myFavoritePokemon = [
  /* ... */
];
const { href, text } = Astro.props;
---

<!-- HTML comments supported! -->{/* JS comment syntax is also valid! */}

<a href={href} class="px-3 py-1 rounded-full bg-zinc-100 text-zinc-500">
  {text}
</a>
